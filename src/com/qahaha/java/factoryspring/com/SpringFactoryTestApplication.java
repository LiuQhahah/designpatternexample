import controller.AppController;

@SpringBootApplication
public class SpringFactoryTestApplication {
    public static void main(String[] args) {
        SpringApplication.run(AppController.class, args);
    }
}
